name: ci

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
          cache: true
          cache-dependency-path: |
            **/*.csproj
            **/*.props
            **/*.targets
            PrintMeter.sln

      - name: Restore
        run: dotnet restore PrintMeter.sln --nologo

      # WinUI XamlCompiler often exits 1 without printing the real error on stdout/stderr — use detailed
      # MSBuild verbosity + binaryLogger; on failure attach obj/**/* (includes XAML compiler temp files).
      - name: Build
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force artifacts | Out-Null
          dotnet build PrintMeter.sln -c Release --no-restore -v:d -bl:artifacts/ci.binlog

      - name: Test
        run: dotnet test PrintMeter.sln -c Release --no-build --collect:"XPlat Code Coverage"

      - name: Upload Windows build diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-build-diagnostics-${{ github.run_attempt }}-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            artifacts/ci.binlog
            src/PrintMeter.App/obj/**/*
