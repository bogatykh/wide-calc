<Window x:Class="PrintMeter.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PrintMeter — длина печати по PDF"
        Height="720"
        Width="1100"
        WindowStartupLocation="CenterScreen">
  <DockPanel Margin="12">
    <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,12">
      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <Button Content="Выбрать PDF…" Command="{Binding PickFilesCommand}" Margin="0,0,8,0" Padding="12,6" />
        <Button Content="Выбрать папку…" Command="{Binding PickFolderCommand}" Margin="0,0,8,0" Padding="12,6" />
        <Button Content="Считать" Command="{Binding AnalyzeCommand}" Margin="0,0,8,0" Padding="12,6" />
        <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="0,0,8,0" Padding="12,6" />
        <Button Content="Экспорт CSV" Command="{Binding ExportCsvCommand}" Margin="0,0,8,0" Padding="12,6" />
        <Button Content="Экспорт XLSX" Command="{Binding ExportXlsxCommand}" Padding="12,6" />
      </StackPanel>
      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <CheckBox Content="Рекурсивно искать PDF в папке" IsChecked="{Binding RecursiveFolders}" VerticalAlignment="Center" Margin="0,0,16,0" />
        <CheckBox Content="UTF-8 BOM для CSV (Excel RU)" IsChecked="{Binding Utf8BomForCsv}" VerticalAlignment="Center" />
      </StackPanel>
      <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" />
      <TextBlock Margin="0,6,0,0" FontWeight="SemiBold" Text="{Binding TotalLengthMeters, StringFormat='Всего, м: {0:F3}'}" />
      <ProgressBar Height="16" Margin="0,8,0,0" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" />
    </StackPanel>

    <Grid DockPanel.Dock="Bottom" Height="120" Margin="0,12,0,0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" FontWeight="Bold" Text="Сводка по форматам" Margin="0,0,0,4" />
      <TextBox Grid.Row="1"
               Text="{Binding SummaryByFormat}"
               IsReadOnly="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto"
               FontFamily="Consolas" />
    </Grid>

    <DataGrid ItemsSource="{Binding Rows}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True"
              HeadersVisibility="Column">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Файл" Binding="{Binding FilePath}" Width="*" />
        <DataGridTextColumn Header="Страниц" Binding="{Binding PageCount}" Width="80" />
        <DataGridTextColumn Header="Длина, м" Binding="{Binding TotalLengthMeters}" Width="100" />
        <DataGridTextColumn Header="По форматам" Binding="{Binding FormatsSummary}" Width="2*" />
        <DataGridTextColumn Header="Ошибка" Binding="{Binding Error}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</Window>
